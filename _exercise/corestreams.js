const {
  createReadStream, 
  createWriteStream
} = require("fs")

const {connect, createServer} = require("net");

const {connect, createServer} = require("tls");

process.stdin
process.stdout
process.stderr


